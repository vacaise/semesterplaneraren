
// Create a simple file for tooltip types used in the ColorSchemeDefinition

export interface TooltipStyles {
  background: string;
  text: string;
  border?: string;
}

export interface TailwindColorStyles {
  base: string;
  hover?: string;
  active?: string;
  text?: string;
  border?: string;
  bg?: string;
}
